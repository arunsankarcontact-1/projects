apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.fullname" . }}
spec:
  selector:
    app: {{ include "app.fullname" . }}-{{ .Values.activeColor }}
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
